import Nav from './Nav';

const Header = () => {
  return (
    <header className='grid-header'>
      <img src='Logo.jpg' alt='Restaurant logo' />
      <Nav />
    </header>
  );
};

export default Header;
